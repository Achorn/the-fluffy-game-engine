import State from "./State";

export default class TitleMenu extends State {
  constructor() {
    super();
  }
  update(deltaTime) {
    // have bouncing fluffies in background in background
    //have menu in forground
    // play
    // options
    // about
  }
  draw(ctx) {}
}
